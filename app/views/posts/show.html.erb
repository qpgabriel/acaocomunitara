<h3><%= @post.title %></h3>
<p><%= @post.body %></p>
<%= link_to "Edit", edit_post_path(@post),:class => 'btn btn-primary'%>
<--%= link_to "Delete", {:action => 'destroy', :method => 'delete', :id => c.id}, :confirm => "Are you sure you want to delete this item?", 
                :class => 'btn btn-danger' %-->
<%= link_to  "Voltar", posts_path,:class => 'btn btn-primary'%>