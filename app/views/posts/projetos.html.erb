<h1>Projetos</h1>
<% @posts.each do |post| %>
	<div class="jumbotron">
		<h3><%= post.title %></h3>
		<p><%= post.body %></p>
		<%= link_to "Show", post_path(post),:class => 'btn btn-primary'%>

	</div>
<% end %>